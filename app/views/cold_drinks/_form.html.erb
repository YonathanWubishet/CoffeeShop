
<%= form_with model: @cold_drink, local: true do |form| %>
 
  <% if @cold_drink.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@cold_drink.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @cold_drink.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
 
  <p>
    <%= form.label :description %><br>
    <%= form.text_area :description %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>